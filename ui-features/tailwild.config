## Objetivo

Este arquivo (`tailwind.config.ts`) conecta o **Design System global (global.css)** com o **framework TailwindCSS**.

Ele serve como uma “ponte” entre o **código** e os **tokens de design**, garantindo que todas as cores, fontes e tamanhos sigam o mesmo padrão visual do projeto.

Em resumo:

> Tudo que está no global.css (cores, tipografia, espaçamento, sombras etc.) é lido automaticamente pelo Tailwind.
> 

## 🧩 Estrutura geral

O arquivo é dividido em blocos:

1. **Dark mode + paths do projeto** (onde o Tailwind vai procurar classes)
2. **Extensões de tema (cores, fontes, espaçamento, etc.)**
3. **Plugins (opcionais)**

---

## Cores (Brand + Neutras)

Todas as cores vêm direto das variáveis do `global.css` (`--verde-primario`, `--cinza-claro` etc.).

| Nome | Token CSS | Função no sistema |
| --- | --- | --- |
| brand.primary | `--verde-primario` | Cor principal da marca |
| brand.secundary | `--preto`  | Cor secundária da marca |
| grayx.section | `--cinza` | Fundos de seções e cards |
| base.white | `--branco` | Fundo base |
| Gradient | `— Verde e Preto` | xx |

🧠 **Como usar no Tailwind:**

```html
<div class="bg-brand-primary text-base-white">Botão Verde</div>

```

---

## 🅰️ Tipografia (Fontes e pesos)

- Fonte padrão: **Poppins**
- Tudo o que o Tailwind lê vem de `-font-sans` do `global.css`.

### Hierarquia tipográfica

| Código | Tamanho (global.css) | Peso | Uso |
| --- | --- | --- | --- |
| `text-h1` | `--t1-size` | `font-t1` (800) | Títulos principais |
| `text-h2` | `--t2-size` | `font-t2` (700) | Subtítulos |
| `text-h4` | `--t4-size` | `font-t4` (600) | Placeholders/Subtextos |
| `text-h8` | `--t8-size` | `font-t8` (500) | Labels/Microtextos |
| `text-body` | `--body-size` | `font-normal` | Texto corrido |

🧠 **Como usar:**

```html
<h1 class="text-h1 font-t1 tracking-t1 leading-h1">
  Kanban Comercial
</h1>

```

---

## 📏 Espaçamentos e Radius

| Token | Valor (global.css) | Uso comum |
| --- | --- | --- |
| `s1` | 4px | espaçamentos pequenos |
| `s2` | 8px | entre ícones e textos |
| `s3` | 12px | margens internas médias |
| `s4` | 16px | padding padrão |
| `s6` | 24px | cards e blocos |
| `s8` | 32px | seções completas |

🧠 **Como usar:**

```html
<div class="p-s4 rounded-lg shadow-md">Conteúdo</div>

```

### Bordas arredondadas

| Token | Valor | Aplicação |
| --- | --- | --- |
| `md` | 12px | Inputs |
| `lg` | 16px | Botões |
| `xl` | 20px | Cards |
| `pill` | 9999px | Botões ovais (estilo cápsula) |

---

## ☁️ Sombras (Shadows)

| Token | Descrição |
| --- | --- |
| `shadow-sm` | leve, quase imperceptível |
| `shadow-md` | padrão para hover |
| `shadow-lg` | destaque ou card ativo |

🧠 **Como usar:**

```html
<div class="shadow-lg hover:shadow-md transition-shadow">Card</div>

```

---

## ⚡ Transições e movimentos

| Nome | Descrição |
| --- | --- |
| `ease-brand` | curva de movimento da marca (cubic-bezier suave) |
| `duration-fast` | transições rápidas (~180ms) |
| `duration-normal` | transições médias (~240ms) |

🧠 **Como usar:**

```html
<button class="transition-colors duration-normal ease-brand hover:bg-brand-hover">
  Salvar
</button>

```

---

## 🧱 Como o Tailwind entende tudo isso

Cada uma das suas variáveis (`--t1-size`, `--verde-primario`, etc.) é “traduzida” dentro do Tailwind para que o desenvolvedor use classes simples, sem precisar lembrar os hex ou pixels.

**Exemplo real:**

```html
<buttonclass="
    bg-brand-primary text-base-white
    text-h1 font-t1 leading-h1 tracking-t1
    rounded-lg px-s4 py-s2
    transition-colors duration-normal ease-brand
    hover:bg-brand-hover
  "
>
  Salvar
</button>

```

➡️ Essa linha acima usa **8 tokens diferentes** (cores, tamanhos, tipografia, motion, espaçamento) sem repetir nenhum valor manual.

---

## 💡 Filosofia do config

1. **Centralizar o estilo no global.css**
    
    → O Tailwind só referencia as variáveis.
    
2. **Nomes semânticos e claros**
    
    → Ex.: `bg-brand-primary`, `text-h4`, `tracking-t1`.
    
3. **Atualização única, impacto global**
    
    → Se `--t1-size` muda, todos os `<h1>` e botões ligados a ela mudam juntos.
    
4. **Padronização entre devs**
    
    → Todos usam a mesma linguagem visual.
    

---

## 🧭 Resumo visual (mental map)

```
global.css  →  tailwind.config.ts  →  componentes React
 (tokens)        (tradução)             (uso prático)

```

**Troque um token → o projeto inteiro atualiza.**

Componnents Padrões: 

Inputs: 

Buttons: 

 1 - Primária

 2 - Secundária